---
import { Image } from "astro:assets";
import { Navigation } from "./Navigation";
import { MobileSheet } from "./MobileSheet";
import logo from "../../assets/img/logo/logo.png";

const pathname = new URL(Astro.request.url).pathname;
---
<header
  id="site-header"
  class="absolute top-0 left-0 z-20 w-full bg-transparent transition"
  transition:animate="none"
>
  <div
    class="mx-auto my-1 flex items-center justify-between gap-6 px-5 lg:container lg:max-w-7xl"
  >
    <!-- LOGO -->
    <a href="/" aria-label="Home page" rel="prefetch" class="shrink-0">
      <Image
        src={logo}
        width="370"
        height="70"
        alt="Vulnerable Support Charitable Trust logo"
        class="header-logo h-auto"
        fetchpriority="high"
        loading="eager"
      />
    </a>
    <!-- DESKTOP NAVIGATION -->
    <div class="hidden lg:block">
      <Navigation pathname={pathname} client:visible />
    </div>
    <!-- MOBILE MENU -->
    <div class="lg:hidden">
      <MobileSheet client:visible />
    </div>
  </div>
</header>

<style is:global>
  /* ========================================
     HEADER
     ======================================== */
  #site-header {
    background: #ffffff !important;
  }

  /* LOGO */
  #site-header .header-logo {
    filter: brightness(0);
  }

  /* NAVIGATION PRINCIPALE */
  #site-header nav a,
  #site-header nav button {
    color: #171717 !important;
  }
  #site-header nav a:hover,
  #site-header nav button:hover {
    color: #171717 !important;
  }

  /* BOUTON VOLUNTEER — reste orange, texte blanc */
  #site-header nav a[class*="rounded-full"]:first-of-type {
    background: #f47b20 !important;
    color: #ffffff !important;
  }
  #site-header nav a[class*="rounded-full"]:first-of-type:hover {
    background: #f47b20 !important;
    color: #ffffff !important;
  }

  /* BOUTON DONATION — reste noir, texte blanc */
  #site-header nav a[class*="rounded-full"]:last-of-type {
    background: #171717 !important;
    color: #ffffff !important;
  }
  #site-header nav a[class*="rounded-full"]:last-of-type:hover {
    background: #171717 !important;
    color: #ffffff !important;
  }

  @media (max-width: 1023px) {
    #site-header {
      background: #ffffff !important;
    }
    #site-header .header-logo {
      filter: brightness(0);
    }
  }
</style>